version: 2
jobs:
  install:
    docker:
      - image: circleci/node:latest
    steps:
      - checkout
      - run:
          name: Install dependencies
          command: yarn
      - save_cache:
          key: dependency-cache-{{ checksum "yarn.lock" }}
          paths:
            - node_modules
  pkg-ok:
    docker:
      - image: circleci/node:latest
    steps:
      - checkout
      - restore_cache:
          key: dependency-cache-{{ checksum "yarn.lock" }}
      - run:
          name: Install dependencies
          command: yarn
      - run: yarn pkg-ok
  lint:
    docker:
      - image: circleci/node:latest
    steps:
      - checkout
      - restore_cache:
          key: dependency-cache-{{ checksum "yarn.lock" }}
      - run: yarn lint
  test:
    docker:
      - image: circleci/node:8
    steps:
      - checkout
      - restore_cache:
          key: dependency-cache-{{ checksum "yarn.lock" }}
      - run: yarn test
  test6:
    docker:
      - image: circleci/node:6
    steps:
      - checkout
      - restore_cache:
          key: dependency-cache-{{ checksum "yarn.lock" }}
      - run: yarn test
  testNext:
    docker:
      - image: circleci/node:latest
    steps:
      - checkout
      - restore_cache:
          key: dependency-cache-{{ checksum "yarn.lock" }}
      - run: yarn test

  release:
    docker:
      - image: circleci/node:latest
    steps:
      - checkout
      - restore_cache:
          key: dependency-cache-{{ checksum "yarn.lock" }}
      - run: |
          yarn release

  publish:
    docker:
      - image: circleci/node:latest
    steps:
      - checkout
      - restore_cache:
          key: dependency-cache-{{ checksum "yarn.lock" }}
      - run: |
          SEMVER=$(echo "v1.0.1-beta" | cut -c2-)
          yarn publish --new-version $SEMVER

workflows:
  version: 2
  build_lint_test:
    jobs:
      - install
      - pkg-ok: 
          requires:
            - install
      - lint:
          requires:
            - install
      - test:
          requires:
            - install
      - test6:
          requires:
            - install
      - testNext:
          requires:
            - install
      - release:
          requires:
            - pkg-ok 
            - lint
            - test
            - test6
            - testNext
          filters:
            branches:
              ignore: /.*/
            # only act on version tags
            tags:
              only: /^v.*/
      - publish:
          requires:
            - pkg-ok 
            - lint
            - test
            - test6
            - testNext
          filters:
            branches:
              ignore: /.*/
            # only act on version tags
            tags:
              only: /^v.*/
